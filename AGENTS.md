# AGENTS.md — 入口（codex 会自动读取本文件）

> ⚠️ **本仓库的完整协作规则在 [`rules.md`](rules.md)。**
> **在对仓库做任何操作之前，你必须先完整读取 `rules.md`，并严格遵守其中全部规则。**
> 本文件只是入口摘要；如与 `rules.md` 有任何冲突，一律以 `rules.md` 为准。

## 第一步（必做）

1. 打开并**完整阅读** `rules.md`：写锁协议、精确 git 步骤、租约防死锁、请求机制、红线全在其中。
2. 确认「我是 `agent_A` / `agent_B` / `agent_C` 中的哪一个」，全程只用这个身份。
3. 然后再开始任何读 / 写 / 提交操作。

## 三条铁律（摘要，完整内容见 rules.md）

1. **写共享资源先加写锁（W）**：改动 `code/`、`results/`、`discussion/`、`files/` 之前，必须按 `rules.md` 第 3 节的写锁协议加**写锁 W**；**读取免锁**，但要先查目标路径有没有被活跃 W 锁盖住，有则暂缓读。不查锁就写 = 违规。
2. **只写自己的请求板**：`requests/agent_<你>.md` 只有你本人能写，别人的只读；请求区免锁。
3. **必须 push 才算数**：本地 `commit` 不等于生效，只有 `git push` 成功后，锁和成果才对其他 agent 可见。
