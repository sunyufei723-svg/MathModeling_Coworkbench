# problem.md — 题目理解与拆解

> 共享文件：**读取免锁**（只查 `discussion` 有无活跃写锁 W）；写需 `discussion` 写锁 W（见 rules.md 第 5 层 / docs/5-concurrency.md）。
> 目的：把赛题读透，形成全队统一的"问题定义"，避免各做各的。

## 0. 选题
- 选定题号：**A 题（药材的烘干问题）**
- 一句话概括题目要解决什么：为中药材**热风烘干**过程建立"传热–传质耦合"数理模型，用**数值仿真**给出药材**温度**与**水分浓度（干基含水率）**随时间/空间的演化规律，以低成本仿真替代高成本、长周期的传统试验优化，支撑烘干工艺参数选取。
- 题干背景（agent_A 2026-09-10T10:25Z 摘录）：干燥是决定中药材成品品质的关键工序；热风烘干主要含**预热平衡**与**恒温干燥**两个阶段，通过**调控烘房温湿环境**完成干燥；工艺参数不当易致干燥效率低、能耗高、品质不稳；传统试验优化成本高、周期长，需借助数理分析与数值仿真得到干燥规律。
- 备注：§0/§4/§5 为题干背景级通识；§1/§2/§3 已补【问题1】【问题2】拆解（agent_A，含模型骨架；问1@10:32Z、问2@10:58Z）；问题3–4 待后续拆解补入。

## 1. 已知 / 给定
> 按小问分组登记；本次先填【问题1】（agent_A 2026-09-10T10:32Z，读附件1/附录2 后）。

**【问题1】预热平衡阶段（0–1800 s）· 已知条件**
- 几何：药材近似圆柱，长 L=25 cm、半径 R=2 cm（=0.02 m）。
- 初始条件（t=0，空间均匀）：药材温度 T₀=28 °C；水分浓度（干基含水率）C₀=2.55 kg/kg。
- 边界驱动（烘房环境，随时间变）：烘房温度 T_env(t)、烘房水分浓度 C_env(t) —— 见 **附件/附件1.xlsx**（Sheet1，3 列：`时间/s`、`温度/°C`、`水分浓度/(kg/kg)`；t=0→14400 s、间隔 60 s、241 个数据点）。问1 只用 **0–1800 s** 段（t=0,60,…,1800 共 31 点），数值求解时按时间步**线性插值**取用。
  - 抽样：t=0 → T_env=28 °C（恰=药材初温，初期净热流≈0）、C_env=0.01963 kg/kg（远低于 C₀=2.55 → 初期脱水驱动力大）。
- 物性参数（**附录2**，问1 专用；除 D 外均为常数）：
  - ρ=820 kg/m³；cp=2600 J/(kg·K)；k=0.36 W/(m·K)
  - 对流换热系数 h=25 W/(m²·K)；对流传质系数 hm=8×10⁻⁷ m/s
  - 水分扩散系数经验式 **D(C)=7×10⁻⁹·exp(−0.89·C) m²/s**（仅随 C 变，不依赖 T）
- 输出模板：**附件/附件3/result1.xlsx**（两工作表「温度」「水分浓度」；A 列=时间/s，第 1 行=到中心距离/cm）。
- 数据存放：`附件/` 已被 `.gitignore` 忽略（仅本地/网盘）。如需入库复现，把 `附件1.xlsx`、`result1.xlsx` 复制到 `files/raw/`（均为小文件，可入库）——是否复制待 decisions.md 定。

**【问题2】整个烘干过程（预热平衡 + 恒温干燥，约 2–3 天）· 已知条件**（agent_A 2026-09-10T10:58Z，读附录3 后）
- 几何/初值：沿用问1（圆柱 L=25 cm、R=2 cm=0.02 m；T₀=28 °C、C₀=2.55 kg/kg，均匀）；**问2 半径固定**（收缩属问4）。
- 阶段：预热平衡 + 恒温干燥，「参数有所不同」——但题面「相关经验公式统一采用附录3」，故**材料经验公式两阶段一致**，不同的是**烘房温湿边界**（预热段随时间变、恒温段恒定）。
- 烘房边界 T_env(t)/C_env(t)：报告窗口 **0–3 h 完全落在附件1（0–4 h、间隔 60 s）内 ⇒ 问2 输出的边界直接由附件1 线性插值给定，无歧义**；据附件1 烘房温度 0–4 h 持续上升，推断**预热平衡≈升温期（附件1 段）、恒温干燥在其后（>4 h）**——恒温段恒定取值与转换时刻主要影响问3，**待确认**。
- 物性经验公式（**附录3**，问2/问3 共用；均随状态变化）：
  - 密度 ρ(C)=650 + 128·C  [kg/m³]
  - 比热容 cp(C)=1450 + 2736·C/(C+1)  [J/(kg·K)]
  - 热导率 k(C)=0.21 + 0.38·C/(C+1)  [W/(m·K)]
  - 扩散系数 **D(C,T)=2.4×10⁻³·exp(−0.45·C)·exp(−3850/T)  [m²/s]，其中 T 单位为开尔文 K**
- 对流系数：附录3 **未另给** h、hm，需沿用附录2 的 h=25 W/(m²·K)、hm=8×10⁻⁷ m/s（**待确认**）。
- 输出模板：**附件/附件3/result2.xlsx**（两工作表「温度」「水分浓度」；A 列=时间/s，第 1 行=距离/cm）。

## 2. 未知 / 求解目标
- 每个小问的具体目标：
  - **问 1（预热平衡阶段，0–1800 s）**：建立并数值求解药材**温度 T(r,t)** 与**水分浓度 C(r,t)** 的时空演化模型，产出：
    1. 论文表格 **表1（温度）/ 表2（水分浓度）**：t∈{100,300,600,900,1200,1500,1800} s × r∈{0,0.5,1,1.5,2} cm。
    2. 完整结果 **result1.xlsx**：t=0→1800 s 每隔 1 s（1801 个时刻）× r=0→2 cm 每隔 0.1 cm（21 个位置），分「温度」「水分浓度」两表。
    3. 所有结果保留 **4 位小数**。
  - **问 2（整个烘干过程；报告窗口 3 h）**：建立**预热平衡 + 恒温干燥全过程**的 T(r,t)、C(r,t) 耦合模型（物性用附录3），产出：
    1. 论文表格 **表3（温度）/ 表4（水分浓度）**：t∈{0.5,1.0,1.5,2.0,2.5,3.0} h × r∈{0,0.5,1,1.5,2} cm。
    2. 完整结果 **result2.xlsx**：每隔 1 s × 每隔 0.1 cm（r=0→2 cm，21 点）。**时长口径待确认**：按与问1 一致的惯例推断为 **3 h@1 s（10801×21）**；但「整个烘干过程」字样亦可能要求覆盖 2–3 天，建议以 result2.xlsx 模板规模 / 队内确认为准。
    3. 结果保留 **4 位小数**（问1「下同」）。
  - 问 3：（待拆解，求水分浓度<0.15 kg/kg 的烘干时长）
  - 问 4：（待拆解，考虑半径收缩，用附件2 + 附录4）

**问1 模型骨架（据附录2 推导 · agent_A 初拟 2026-09-10T10:32Z，待 decisions.md 确认）**
- 域/坐标：一维径向柱坐标，r∈[0, R=0.02 m]、t∈[0,1800 s]；轴对称、忽略轴向（假设 B1）。
- 传热方程（ρ,cp,k 常数 → **线性**）：`∂T/∂t = α·(∂²T/∂r² + (1/r)·∂T/∂r)`，α=k/(ρ·cp)≈1.69×10⁻⁷ m²/s。
- 传质方程（D 随 C 变 → **非线性**扩散）：`∂C/∂t = (1/r)·∂/∂r( r·D(C)·∂C/∂r )`，D(C)=7×10⁻⁹·exp(−0.89·C)。
- **结构观察（重要）**：附录2 中 ρ/cp/k 常数、D 仅依赖 C ⇒ 问1 的 **T 场与 C 场数学上解耦**（可各自独立求解），比通用耦合假设 A1 简单；附录2 未给汽化潜热，倾向传热方程**不加蒸发源项**（纯导热）。是否引入潜热耦合项 → 待全队确认。
- 初始条件：T(r,0)=28 °C；C(r,0)=2.55 kg/kg（均匀）。
- 边界条件：
  - 中心 r=0（对称/无通量）：∂T/∂r=0、∂C/∂r=0。
  - 表面 r=R（第三类）：热 `-k·∂T/∂r|_R = h·(T(R,t) − T_env(t))`；质 `-D(C)·∂C/∂r|_R = hm·(C(R,t) − C_env(t))`；T_env/C_env 由附件1 插值。
- 数值方案（建议）：有限差分/有限体积离散；r=0 用对称性消 1/r 奇点（L'Hôpital → 2·∂²/∂r²）；显式须满足稳定性（取 Δr=0.1 cm、Δt=1 s 时热扩散数 α·Δt/Δr²≈0.17<0.5 稳定；传质 D 更小更稳），或用隐式/Crank–Nicolson；D(C) 非线性按当前步 C 更新或迭代；建议加密网格验证**网格无关性**。
- 单位：问1 中 T 用 °C 即可（无辐射、D 不依赖 T，边界只用温差）；长度 cm→m；问2/3/4 的 D 依赖 T(K) 时再换算绝对温度。

**问2 模型骨架（据附录3 推导 · agent_A 初拟 2026-09-10T10:58Z，待 decisions.md 确认）**
- 域/坐标：一维径向柱坐标，r∈[0, R=0.02 m]；时间覆盖整个烘干过程（报告 0–3 h；若需全程则到 2–3 天）。半径固定。
- 传热方程（**变物性 → 非线性**）：`ρ(C)·cp(C)·∂T/∂t = (1/r)·∂/∂r( r·k(C)·∂T/∂r )`，ρ/cp/k 由附录3 随 C 更新。
- 传质方程（**D 随 C、T 变 → 强非线性**）：`∂C/∂t = (1/r)·∂/∂r( r·D(C,T)·∂C/∂r )`，D=2.4×10⁻³·e^(−0.45C)·e^(−3850/T)，**T 用 K**。
- **耦合结构（与问1 的关键差别）**：C →(ρ,cp,k)→ T 方程；T →D→ C 方程 ⇒ **T、C 双向耦合**，须**联立/迭代**求解（问1 解耦）。
- 初始条件：T(r,0)=28 °C；C(r,0)=2.55 kg/kg（均匀）。
- 边界条件：
  - 中心 r=0（对称/无通量）：∂T/∂r=0、∂C/∂r=0。
  - 表面 r=R（第三类）：热 `-k(C)·∂T/∂r|_R = h·(T(R,t) − T_env(t))`；质 `-D(C,T)·∂C/∂r|_R = hm·(C(R,t) − C_env(t))`；T_env/C_env 由附件1 插值（报告窗 0–3 h 全在附件1 内）；若模拟 >4 h 恒温段则取常数（待确认）。
- 潜热/源项：附录3 未给汽化潜热，与问1 一致**倾向传热不加蒸发源项**（纯变物性导热）；是否加潜热耦合 → 待全队确认（恒温干燥段蒸发强，此项影响更大）。
- 数值方案（建议）：变系数 + 强非线性 + 双向耦合 ⇒ 每时间步**联立迭代**（如：猜 T→解 C→更新 ρ/cp/k/D→解 T→迭代至收敛；或全隐式 Newton 联立）；空间有限差分/体积，r=0 用对称消 1/r 奇点；时间用隐式/Crank–Nicolson 保稳定（步数多）；D 的 Arrhenius 项 e^(−3850/T) 对 T 极敏感，注意 T(K) 换算与迭代收敛；建议变步长 + 网格无关性验证。
- 单位：**务必 T(°C)→T(K)=T+273.15** 再代入 D；长度 cm→m；结果按 °C / (kg/kg)、4 位小数输出。

## 3. 约束条件
**【问题1】**（agent_A 2026-09-10T10:32Z）
- 硬性约束（必须满足）：
  - 时间域 t∈[0,1800] s；空间域 r∈[0,2] cm（半径固定，问1 不启用收缩）。
  - 输出严格按 **表1/表2** 指定时间点×距离点给值，并把 **1 s × 0.1 cm** 完整网格写入 result1.xlsx 模板（两工作表、A 列时间、第 1 行距离）。
  - 所有结果保留 **4 位小数**。
  - 初/边界条件、物性、D(C) 公式严格取自 **附件1 + 附录2**，不自造参数。
- 软性约束 / 优化目标：数值解**稳定、收敛、网格无关**（加密 Δr/Δt 结果不再明显变化）；预热阶段仅 1800 s，计算量可控。

**【问题2】**（agent_A 2026-09-10T10:58Z）
- 硬性约束（必须满足）：
  - 空间域 r∈[0,2] cm（半径固定，问2 不启用收缩）；时间域至少覆盖报告的 0–3 h（模型面向整个烘干过程 2–3 天）。
  - 物性 ρ/cp/k/D **严格用附录3 公式**（D 中 T 必须为开尔文）；h、hm 附录3 未给，沿用附录2 值须标注为假设。
  - 输出按 **表3/表4**（t 每 0.5 h、r∈{0,0.5,1,1.5,2} cm）+ **result2.xlsx**（1 s × 0.1 cm 完整网格，时长口径待确认）；保留 **4 位小数**。
  - 烘房边界：报告的 0–3 h 取自附件1 插值（无歧义）；>4 h 恒温段的恒定值 / 转换时刻按队内确认（主要影响问3）。
- 软性约束 / 优化目标：耦合迭代**收敛**且**网格/时间步无关**；长时间积分**稳定**、计算量可控（必要时变步长）。

## 4. 关键假设
> 建模必须显式列出假设，后续在 decisions.md 确认。
> 下列假设由 **agent_A（2026-09-10T10:25Z）仅依据题干背景**（热风烘干；预热平衡+恒温干燥两阶段；调控烘房温湿环境；需数理分析与数值仿真求干燥规律）提出，目的是把物理过程理想化为**可数值求解的传热–传质耦合模型**；不依赖问题1–4 的具体数值，物性/经验公式待阅读问题与附录后套用。

**A. 过程本质（传热–传质耦合）**
- 假设 A1：烘干是**传热与传质耦合**过程——热风向药材供热（升温 + 供水汽化潜热），药材内部水分受热向表面迁移、在表面被气流带走；**温度场 T 与含水率场 C 相互耦合**，均随时间与空间变化。

**B. 几何与维度简化**
- 假设 B1：药材形状规则（近似圆柱），**长度远大于半径**，忽略轴向与端面效应，简化为**一维径向、轴对称**问题（状态量只沿"到中心距离 r"变化），把模型降为 1D 以便数值求解。

**C. 内部传递机理**
- 假设 C1：内部水分迁移以**扩散**为主，服从 **Fick 第二定律**，用**有效水分扩散系数 D** 刻画，D 随 C（及 T）变化。
- 假设 C2：内部热量传递以**热传导**为主，服从 **Fourier 定律**，用热导率 k 刻画；ρ、cp、k 等物性视为随 C/T 变化的经验量。
- 假设 C3：水分在内部的**汽化相变与潜热**作集总/简化处理（并入等效比热或源项），不显式追踪两相流动。

**D. 边界与外部环境**
- 假设 D1：药材表面与烘房热风间采用**第三类（对流/Robin）边界**——对流换热系数 h 描述表面换热、对流传质系数 hm 描述表面水分交换。
- 假设 D2：烘房温湿环境为**给定、可控的外边界**（按两阶段设定曲线变化）；烘房内空气温湿度均匀、风速稳定，忽略其空间不均匀性。
- 假设 D3：忽略**热辐射**换热与药材–托盘接触导热（常压热风干燥以对流为主）。

**E. 初始条件**
- 假设 E1：烘干初始时刻，药材**温度与含水率在空间上均匀分布**（取给定初值）。

**F. 阶段划分**
- 假设 F1：全程分**预热平衡**与**恒温干燥**两阶段——预热阶段以升温逼近烘房温度为主、水分变化相对小；恒温阶段温度趋稳、以持续脱水为主；两阶段边界条件与物性取值可不同，**分别建模再衔接**。

**G. 其它理想化**
- 假设 G1：**单根代表性药材独立干燥**，忽略药材间遮挡/堆叠对气流与温湿场的影响。
- 假设 G2：**干基质量守恒**，C = 水分质量 / 干物质质量；除脱水外忽略其它成分（如挥发油）质量损失。
- 假设 G3：物性参数与经验公式在适用范围内**连续、光滑**，便于离散与迭代求解。
- 假设 G4（可切换档位）：**尺寸/收缩**——一般先设药材尺寸不变（刚性）以简化；需精确刻画干燥后期或题目专门要求时，再引入随含水率变化的半径。
- 假设 G5（数值层面）：对时间/空间**离散**（有限差分/有限体积），物性随 C、T 更新，T 场与 C 场**耦合迭代**求解，并满足稳定性/收敛条件（扩散数、CFL 等）。

> **问1 特化（agent_A 2026-09-10T10:32Z）**：附录2 给问1 的 ρ/cp/k 为常数、D 仅依赖 C，故上述通用假设在问1 简化为「T 场与 C 场**解耦**、传热无蒸发源项」；尺寸固定，不启用 G4 收缩档位。详见 §2「问1 模型骨架」。

> **问2 特化（agent_A 2026-09-10T10:58Z）**：附录3 令 ρ/cp/k 随 C 变、D 随 C 与 T(K) 变 ⇒ 通用假设 A1「T/C 耦合」在问2 **成立且为强非线性双向耦合**（不同于问1 的解耦），须联立迭代求解；尺寸仍固定（不启用 G4）；潜热源项仍缺参数、暂不加（与问1 同，待确认）。详见 §2「问2 模型骨架」。

## 5. 问题类型判断
- 属于哪类模型（优化 / 预测 / 评价 / 微分方程 / 图论 / 统计 / 机器学习 / 仿真 …）：**偏微分方程 + 数值仿真**（非线性耦合的 Fourier 热传导 + Fick 扩散方程组，物性/系数随含水率 C、温度 T 变化），求解温度场与含水率场的时空演化。
- 可能的难点（agent_A 初判，待读问题后细化）：T 场与 C 场的非线性耦合迭代、多阶段（预热/恒温）参数切换与衔接、长时间数值稳定性、大量时空网格结果的输出。

## 6. 分工建议
> 三人如何切分（建模 / 编程求解 / 论文写作 常并行推进）：
- agent_A：
- agent_B：
- agent_C：

---
_更新记录：每条修改请署名 + UTC 时间戳。_
