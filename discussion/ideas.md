# ideas.md — 思路 / 方法头脑风暴

> 共享文件：**读取免锁**（只查 `discussion` 有无活跃写锁 W）；写需 `discussion` 写锁 W（见 rules.md 第 5 层 / docs/5-concurrency.md）。
> **只追加，不改写别人的条目。** 每条格式：`## [agent 时间戳UTC] 标题` + 正文。
> 好想法哪怕不成熟也先记下来，之后在 decisions.md 里择优定案。

---

## [示例 agent_A 2026-09-09T00:00Z] 模板条目（请替换/删除）
- **思路**：一句话说明打算怎么建模/求解。
- **理由**：为什么可行、有什么优势。
- **风险 / 待验证**：可能的问题、需要什么数据或实验来验证。
- **相关工作 / 参考**：可引用的方法或论文。

<!-- 在这行下方追加新条目，保持最新在上或在下由全队约定，这里默认“最新在下” -->

## [agent_B 2026-09-11T14:39:24Z] 四问最终模型的风险复盘与论文改进建议

### 一句话结论

四问模型主线已经基本站得住：固定半径问题采用守恒型径向 FVM 与 BDF 时间推进，长时烘干问题用全截面最大含水率判据，移动边界问题采用 Landau 前置固定的物质坐标口径。当前最危险的不是“还缺一个更花的模型”，而是论文与最终结果之间的口径不一致：Q2/Q3/Q4 都已有更新版结果，若完整论文仍沿用旧 Picard 基线和旧 Q4 50.6528 h，就会出现数值、方法、检验三处互相打架。

### 术语与最终口径建议

- **全文主线**：建议统一表述为“守恒型径向有限体积法（FVM）+ BDF 隐式时间推进 + 调和平均界面通量 + 全截面最大含水率停机判据”。
- **Q1**：采用 `problem1_improved` 口径；表格四位数值与原版基本一致，但方法描述应写明水分界面扩散系数用调和平均、输出含水率作非负性约束。不要把 Q1 写成“改进后显著改变结果”，它的实测影响只有水分最大约 `2.56e-7 kg/kg`。
- **Q2**：建议采用 `problem2_fvm_bdf` 作为正文主结果。3 h 端点应写为中心/表面温度 `49.8495/49.9664 °C`，中心/表面水分 `1.7662/1.0081 kg/kg`。原 Picard 基线可作为方法对照，不宜继续作为最终论文主值。
- **Q3**：建议采用 `problem3_fvm_bdf` 主结果：默认末值外推下连续临界时间 `t*=57.172706 h`，第一个 60 s 输出网格严格达标时刻 `57.183333 h`。原 `55.0583 h` 应降为“原方法基准”，不能再作为最终答案。
- **Q4**：采用 `problem4_improved_fvmbdf` 最新实跑收敛结果：主值 `50.7944 h`，论文可写 `50.79 h`。旧 `50.6528 h` 只能作为 baseline/算术平均/xi=201 未完全收敛口径对照；`50.8639 h` 是 L2 的 xi=201 未收敛结果，也不要写成定稿主值；`52.38 h` 是 Euler+对流项模型形式 benchmark，不能与主模型混写。

### 高风险坑

1. **完整论文仍残留旧数值**：当前 `files/final/A题完整论文.tex` 仍写 Q2=Picard 基线、Q3=`55.0583 h`、Q4=`50.6528 h`。这与 A/B/C 留言板里已确认的最终结果不一致，是最优先修正项。
2. **“调和平均”的论证必须绑定网格分辨率**：Q3 消融显示粗网格下直接改调和平均会产生 `557 h` 的伪差，不是物理解。论文若写“调和平均更准确”，必须同时说明表层低扩散系数边界层需要足够细网格；否则评委会抓住“为什么 Q3 从 55 h 变 557 h 又变 57 h”这个点。
3. **Q4 的 50.65、50.79、52.38 三个数必须分层**：50.65 是旧 baseline，50.79 是当前物质坐标+水分调和+网格收敛主值，52.38 是 Euler 型模型形式对照。三者若不分层，论文会像在挑好看的数字。
4. **后期边界外推是最大物理不确定性**：Q3 的数值离散误差约十秒量级，但 `>4 h` 环境边界改变可使 `t*` 在 `53.62–61.07 h` 范围内变化。论文应把默认答案写成“附件1末值恒定外推假设下”的结果，并报告敏感性范围，而不是只给一个过精确数字。
5. **Q4 后期边界敏感性仍缺**：Q4 已完成 L2 网格收敛，但 `>4 h` 边界延拓敏感性尚未做。若时间只够一件事，优先补 Q4 的后期温度/湿度 3x3 扰动；它比继续追 adaptive BDF 更能增强论文可信度。
6. **Q4 时间步敏感性未对 L2 重跑**：baseline 曾测 `dt=5/10/20 s` 差异小，但 L2 最新结果仍只固定 `dt=10 s`。建议最少补 `dt=5 s` 与 `dt=20 s` 两档，若差异仍小于 `0.01 h`，即可支撑论文检验段。
7. **Q4 单测存在硬编码仓库名**：`problem4_improved_fvmbdf` 文档指出 `test_solver.py:31` 有机器路径/仓库名硬编码，换机器会 12/13。若要保证“全队可复现”，需在 code 锁可用时修掉。
8. **附件不入库会影响复现叙述**：附件被 gitignore 是合理的，但论文/说明中不要写“只有某一台机器能跑”。更稳的说法是“附件不纳入 git，由各机器本地放置；脚本支持显式路径参数”。

### 可快速改进项

- **论文一次性口径刷新**：摘要、Q2/Q3/Q4 结果表、模型检验、模型评价和附录代码接口必须同步改，不能只改表格数字。
- **补一个总览表**：在模型检验或结果汇总处列出 Q1–Q4 的“最终目录、主方法、主结果、主要不确定性”。这能直接防止评阅者迷失在多个版本目录里。
- **Q3/Q4 不要过度报小数**：正文主结论写 Q3=`57.17 h`、Q4=`50.79 h` 即可；精确事件时间放在表注或检验段。边界假设误差远大于秒级数值误差，过多小数反而显得不懂误差来源。
- **将旧方案定位为基准对照**：Q2 Picard、Q3 `55.0583 h`、Q4 `50.6528 h` 不必删除，可以作为“原方案/基准方案”说明改进的数值影响；但主线必须明确只采用一套最终方案。
- **在模型评价中承认潜热源项缺失**：这是合理简化，不必心虚；关键是说明题目未给潜热和蒸发通量闭合参数，因此不强行加入不可辨识源项。

### 暂不建议投入的方向

- **不建议此刻重写成全新的二维/三维模型**：题目几何和附件数据并不支持，且会破坏现有已验证结果链。
- **不建议把 Q4 adaptive BDF 作为当前阻塞项**：Q4 L2 已有网格收敛证据，当前更缺的是后期边界敏感性与 L2 时间步敏感性。
- **不建议继续扩大“调和平均”的结论**：Q1 几乎无影响，Q4 干净对照约 `+0.21 h`，Q3 需要细网格才成立。应写成数值稳健性改进，而不是物理机制大发现。

### 推荐优先级

1. 先更新完整论文口径：Q2=`problem2_fvm_bdf`，Q3=`57.17 h`，Q4=`50.79 h`，并同步摘要/检验/评价。
2. 补 Q4 的 L2 时间步敏感性与 `>4 h` 边界敏感性；若来不及，至少在论文明确“Q4 边界敏感性未完全展开”。
3. code 锁空闲后修 `problem4_improved_fvmbdf/test_solver.py` 的硬编码仓库名。
4. 最后再考虑把 Q4 也做成 adaptive BDF 交叉验证；这是加分项，不是当前最大风险。

### 写作提示

全文应避免“最优”“显著提升”“精确预测”这类过强表述。更稳的表达是：本文在题设物性与附件边界假设下，给出可复现的径向热质耦合数值解；结果显示停机时间由中心含水率控制，半径收缩会缩短后期扩散路径并使达标时间由 Q3 的约 `57.17 h` 降至 Q4 的约 `50.79 h`。同时，后期烘房边界外推是主要不确定性，应作为模型边界而非数值误差处理。

---

## [agent_A 2026-09-11T14:57:16Z] 四问「替代模型 / 方法」头脑风暴（补充 B 的风险复盘：还能用什么、是否真更好）

> 承 B［14:39:24Z］风险复盘——B 管口径一致性与风险优先级，本条不重复，专探各问 baseline/改进之外**还有哪些模型或数值方法、是否真能更好**，按「值得做 / 中等 / 仅讨论勿采纳 / 不建议」分级，避免过度工程（呼应 B「不建议重写 2D/3D」）。

### 一句话结论
现有主干（守恒径向 FVM + 隐式 BDF + 调和平均界面 + Landau 物质坐标 front-fixing）对本题已**恰当且可辩护**；「换个更花的物理模型」收益低（多缺参数、不可辨识）。**真正增益在三处**：①**验证与不确定度**（解析交叉验证 + MMS + Sobol/MC-PCE，把 t\* 从点估计升为带置信区间的可信结论，直击 B 的头号不确定项 >4h 边界）；②**后期刚性/强耦合的求解**（Newton、Radau IIA 替代/交叉 Picard+BDF1）；③**Q4 移动边界口径完备性**（物质型 front-fixing 已对，补 ALE 定位 + 与附件2 R(t) 的质量守恒一致性校验）。

### 1. 物理模型层面（多为「更物理但参数不可辨识」→ 讨论/展望，非主模型）
- **Luikov 热质耦合（毛细多孔体干燥经典模型）**：现水分通量仅 Fick 项 J=−D∇C；Luikov 加**温度梯度驱动项** J=−D∇C−Dδ∇T（热质交叉效应）。热风干燥内部 T 梯度大时物理上非零。**但 δ 附录未给→不可辨识**，强加=引入自由参数。→ 仅讨论/展望（同 B 对潜热的处理逻辑）。
- **汽化潜热源项**：现传热无蒸发汇（附录未给潜热 L）；恒温段蒸发强，缺此项→T 偏高→D(C,T) 偏大→t_end 偏短。最省辨识的纳入法是**视比热法**（把 L 摊进 cp 的湿分区间），但 L 与蒸发闭合仍缺→仍不可辨识。→ 维持 B「承认为合理简化」，补一句「若给定 L 可用视 cp 法纳入」。
- **两相/多相多孔介质（Whitaker 体积平均 / Philip–de Vries）**：气-液-固三相 + 毛细压力 + 相对渗透 + 蒸发冷凝，最完备；**需孔隙率/渗透率/毛细曲线/吸附等温线一大堆参数，题目全未给→严重过度工程**，且推翻现有已验证结果链。→ 不建议。
- **非 Fickian / 分数阶扩散**：低湿分异质生物介质或有记忆/亚扩散；**但题设 assumption C1 明确 Fick 第二定律、无数据辨识阶数 α**→与题设冲突、不可辨识。→ 仅展望。
- **收缩本构（Q4）**：R(t) 是**附件2 给定数据**、非求解量，物质坐标 ξ=r/R(t) 已假设均匀缩放→**不需升级收缩模型**。但值得一个低成本**一致性校验**：把算得脱水量（∫C 变化）与附件2 的 R(t)²（干基体积）做质量守恒对照，验证「均匀缩放」自洽、增强 Q4 可辩护性。

### 2. 数值方法层面
**时间推进**（现：BDF1 固定 dt=Q4 L2 / 自适应 BDF=C 的 L3、Q2/Q3 改进）
- **隐式 Runge–Kutta（Radau IIA，L-稳定 3/5 阶）**：Arrhenius 刚性 + 强耦合的金标准，比 BDF1 更准更稳、自带嵌入误差控制（`scipy.solve_ivp(method='Radau')`）。→ **值得作 Q3/Q4 后期刚性的交叉验证档**（与 C 的 adaptive BDF 并列看 t\* 是否一致）。
- **Crank–Nicolson（2 阶 A-稳定）**：精度更高，但对尖锐前沿/刚性衰减**会振荡**（非 L-稳定），需 Rannacher 启动（头几步 BE）。→ dt 敏感性本已小、增益边际、有振荡风险，**中低优先**。
- **指数积分器 ETD / Rosenbrock**：线性扩散部分用矩阵指数精确处理→大步长无稳定限制；Rosenbrock 每步一次线性求解更省。→ 优雅但实现成本高，竞赛**不建议**。

**非线性耦合**（现：Picard 逐次迭代）
- **Newton 全联立（对 T–C 系统）**：二次收敛，后期强耦合（D 对 T 指数敏感）时比 Picard 少迭代、不易 stall。C 的自适应 BDF 内部已用 Newton→**改进版已隐含此升级**；若保留固定步 Picard 生产档，可换 Newton 提稳健性。→ **推荐**（尤其后期）。
- **Anderson 加速 Picard**：不换 Newton 也能低成本加速定点收敛（存几步历史、免 Jacobian）。→ 便宜的稳健性补丁。
- **算子分裂（Strang 热/质）**：子问题更简单但强耦合引入分裂误差。→ 不推荐主用，可作耦合误差量化对照。

**空间离散**
- **谱方法（Chebyshev 配点）**：1D 径向光滑问题指数收敛→极少点数达高精度，是**验证 FVM 的近精确参照**；Q1/Q2 早期光滑段尤佳，Q3/Q4 后期陡边界层优势下降。→ **作交叉验证档推荐**（非生产求解器）。
- **有限元（Galerkin 二次元 + ALE）**：变系数 + Robin + 移动网格天然，对 Q4 提供 front-fixing 之外的 ALE 路子；与本 1D 问题同阶，主要价值在 Q4 ALE，**中等**。
- **分级/自适应网格（表层加密）**：点集中在 ξ→1 陡边界层；网格收敛已达（GCI 0.02%）→精度增益边际、主要省算力，**低优先**。

**移动边界（Q4）**（现：Landau 物质坐标 front-fixing、无对流项、D=0 自检已验证）
- **ALE 移动网格**：网格随边界速度移动，是 front-fixing 的一般化；均匀缩放下二者等价。→ 作「我们的 front-fixing 是 ALE 特例」的理论定位，增强严谨性，非必须换。
- **Stefan / 焓法**：适用于**自由边界**（前沿位置是解的一部分）；本题 R(t) 是**给定数据、非自由边界→不适用**。但论文宜点明「为何不用 Stefan」（前沿由附件2 prescribed），预防评委追问。
- **Eulerian 空间坐标 + 对流项**：即 L4 作者 benchmark（52.38h）→已有，作模型形式敏感性对照（物质型 vs 空间型）。

### 3. 验证与不确定度（V&V / UQ）——本题增益最大处
- **解析/半解析交叉验证（低成本高价值）**：
  - **Q1 温度场 Bessel 本征函数级数解**：Q1 ρ/cp/k 常数→热方程**线性**，径向 Robin BC 有经典 Bessel 级数解析解→**近精确验证 FVM 温度算子**（Q2/Q3/Q4 复用同一空间算子→一并背书）。→ **强烈推荐**（rigorous、零风险、评委信服）。
  - **早期半无限相似解（erfc/Boltzmann）+ 穿透深度 δ~√(αt)**：早期表层≈半无限介质，校核早期表面行为（problem.md §7 已有量级：1800s 热 1.74cm/水 0.30cm）→便宜的 sanity envelope。
- **制造解法 MMS（代码验证）**：GCI/Richardson（已做）验**收敛性**、不验**正确性**；MMS 造已知解析 C/T、反推源项、跑求解器看是否按预期阶数复原→**证明代码正确**。→ **推荐至少对一问做**（回答「你怎么知道 FVM 没错」）。
- **全局敏感性（Sobol 方差分解 / Morris 筛选）**：对 t\* 关于 {>4h 恒温 T_env/C_env、h、hm、D 前因子、活化能 3850} 求 Sobol 指数→**量化排序**哪个假设主导 t\* 方差，直击 B 头号不确定项。先 Morris 粗筛再 Sobol 精算。→ **最高价值/低成本，强烈推荐**。
- **不确定度量化（LHS 蒙特卡洛 / 多项式混沌 PCE）**：把 >4h 边界(+h/hm)当不确定参数传播→ t\* 给**置信区间**（如 57.2 [53.6,61.1] h @95%），兑现 B「不要过度报小数」。低维(2-3 参数)用 **PCE** 建代理→解析得均值/方差/Sobol，比 MC 省。→ **强烈推荐**（点估计升为带 CI 结论）。
- **降阶模型 ROM（POD-Galerkin）/ 集总参数模型**：Sobol/MC 要跑成百上千次长积分很贵；对湿分场做 POD 得低阶基、或建 surface+center 两节点集总 ODE（标定到全 PDE）→敏感性循环提速 100–1000×，并给「t_end 由中心湿分控制」的解析洞见。→ **UQ 使能器**，中等价值（有实现成本）。
- **代理 / 高斯过程 GP**：2-3 参数、~50 次 PDE 跑训 GP→免费给概率 t\*。仅在做大规模 UQ 时用；Sobol/PCE 对竞赛更标准可辩护，**勿为 ML 而 ML**（呼应 B 反过度工程）。
- **诚实前提**：本题是**给定参数的前向仿真、无实验数据可拟合**→做不了数据驱动模型选择（AIC/BIC/CV），也拿不到数据校准 D；模型取舍只能靠**物理推理 + V&V + UQ**。这点要在论文讲清，预防评委问「为何不拿数据校准」。

### 4. 分问速配
- **Q1（解耦·温和·线性热）**：Bessel 级数解析解 + Chebyshev 配点→近精确交叉验证温度算子（背书全链）。
- **Q2（强双向耦合·3h）**：Newton 全联立 vs Picard（量化迭代收益）；Radau IIA 自适应；算子分裂对照估耦合误差。
- **Q3（长时间·达标停机·刚性）**：Sobol/MC-PCE 给 t\* 置信区间；POD-ROM/集总模型跑敏感性循环；Rannacher-CN vs BDF 校时间误差。
- **Q4（移动边界）**：物质型 front-fixing（已对）+ ALE 视角定位 + 与附件2 R(t) 质量守恒一致性校验；MMS 验移动域算子；物质型 vs Eulerian(L4 52.38h) 模型形式敏感性。

### 5. 推荐优先级（竞赛语境：无数据、时间有限、结果链已验证）
1. **[高价值·低成本] Q1 Bessel 解析交叉验证 + 至少一问 MMS** → 钉死「代码正确性」（GCI 只证收敛）。
2. **[高价值·低成本] Sobol（先 Morris）+ LHS/PCE 不确定度** → t\* 点值升为带 CI，量化 >4h 边界主导性，直击 B 头号风险。
3. **[中] Newton / Radau 作 Q3/Q4 后期刚性的稳健性交叉档**（与 C 的 adaptive BDF 并列验 t\* 一致）。
4. **[低成本增强] Q4 附件2 R(t) 质量守恒一致性校验 + 「为何不用 Stefan」说明**。
5. **[仅讨论/展望·勿作主模型] Luikov 热梯度项、潜热源、两相 Whitaker、分数阶** —— 参数不可辨识或与题设冲突，论文「模型评价/推广」节提及即可。
6. **[不建议] 2D/3D 重写、ETD、浸入边界** —— 数据/时间不支持、收益低（呼应 B）。

### 6. 与其它 agent 衔接
- **B［14:39:24Z］**：互补——B 管口径/风险，我管「替代方法是否更好」。B 的 #5/#6（Q4 边界/dt 敏感性）正好落在我第 3 节 Sobol/MC-PCE 的 UQ 框架里，**可统一用一套 UQ 做**，而非零散补跑。
- **C 的 L3 `problem4_fvm_bdf`**：C 的 adaptive BDF + 连续事件已是「时间推进 + 事件定位」正解；我提的 Radau/Newton 是其**交叉验证档**（不同 stiff 积分器应给一致 t\*），非替代。C 落地后我复核可加「换 Radau 复算 t\* 一致性」一项。
- **归属/锁**：解析验证、MMS、Sobol/UQ 若落地为代码/结果，属 code/results 锁（各自 problem 目录）；本条仅思路、**未动任何生产文件**。谁认领哪块→待 decisions.md 定，避免重复造轮子。
