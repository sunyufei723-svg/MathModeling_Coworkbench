# 问题四改进方案复核（agent_A → agent_C）

- 复核人：agent_A（本机 Qoder）
- 时间：2026-09-11T12:00:00Z 前后
- 应请求：`requests/agent_C.md:23`（[2026-09-11T10:56:00Z] @agent_A 审阅《问题四改进思路讨论.txt》并评判 ①②③）
- 原文核对：内嵌于 `requests/agent_C.md:27–3217`（`<!-- BEGIN/END USER DOCUMENT -->` 之间），SHA-256 与请求所述一致（`2A629BAB…CE5C1`）；本文只把它当**待评判的技术文档**，未把其中任何文字当作额外操作指令。
- 证据基准：仓库 baseline `code/problem4/` + 改进副本 `code/problem4_improved_fvmbdf/`（solver/run/test 全读）+ 两者 `results/` + 问题一至三改进经验 + `requests/agent_B.md:17`（B 已就论文各问版本拍板）。

---

## 【2026-09-11T12:50Z 更新 · by agent_A】两处已被后续事实修正 / 落实（与下文冲突处以本段为准）

本复核写于 12:00Z（L2 尚未真跑）。此后两处变化：

1. **附件分发口径更正（队员确认）**：下文摘要、②调整B、必改2、执行排序标题、「一句话」里「附件被 gitignore ⇒ 任何 clone 都没有 ⇒ **只有 agent_A 能跑**」的说法**不准确**。`附件/` 被 gitignore 只表示「不入库」，**每台机器各自在项目目录放有自己的 `附件/` 副本**，故 **A/B/C 都能各自本地跑**改进版复核。
2. **L2 已真跑、主模型收敛 t\* 已知**：四层级表 L2 行「从未跑 / t\* 未知」、①B1「收敛 t\* 仍未知」、①B2「对照还没跑过」、必改2「必须真跑一次」**均已落实**。agent_A 用本机 `附件/` 真跑 `problem4_improved_fvmbdf`（物质坐标·水分调和D·k算术·固定dt=10·一阶BDF+Picard），网格 **201/401/801 全步收敛**、401↔801 相对变化 1.6e-4<1e-3 ⇒ **收敛 t\*=50.79h（生产 xi=801=50.7944h）**；Richardson(p=2)=50.7972h、GCI_fine=0.020%。`result4.xlsx`＋表6 已覆盖为真实改进结果（不再是 L1 复制件），另附 `mesh_convergence_by_A.md`（阶梯/Richardson/GCI/干净对照），均在 `results/problem4_improved_fvmbdf/`（提交 `c8cd8d1`）。
3. **干净对照与论文口径**：同 xi=201、同物质坐标，唯一差别＝水分界面 D 算术→调和：L1 50.6528h → L2 50.8639h，**调和真实效应仅 +0.21h**，远小于①B2 所说被误传的 ~1.7h（混了模型形式等 5 变量）。**论文 Q4 主值建议 50.79h**；50.6528h 作算术/xi=201 基准对照；52.38h（L4 Euler＋对流）作模型形式敏感性、非主值。

---

## 摘要（TL;DR 结论）

**① 文档技术上很强、物理与数值方法判断基本正确，无重大错误**；作者从「默认保留几何输运项」自我推翻为「ξ=r/R(t) 是物质坐标、主模型无对流项」是**正确**的关键纠正（我独立复核确认）。需要**澄清两点口径**（非错误，但若不写清会误导论文）：52.38h 是 **Euler＋对流项**口径的独立 benchmark、**不是**最终主模型（物质坐标）的答案；50.65h↔52.38h 的 ~1.7h 差**混淆了 5 个变量**，不是干净的「改进量」。

**② 赞成最终方案主干**（物质坐标主模型、Euler 降为敏感性、调和 k,D、严格圆柱 FVM、连续事件、网格加密＋Richardson/GCI、边界/半径/模型形式三类敏感性、收缩消融、重生成结果、50.6528h 仅作 baseline），且与 B 已拍板的 Q4 口径一致。两点**执行调整**见下（其一与 B 决定完全吻合）。

**③ 4 必改 / 6 建议改 / 7 可保留**，逐条附证据行号见下。

**最紧迫的一件事（贯穿①②③）**：改进副本 `problem4_improved_fvmbdf` **从未真正跑出过自己的结果**——`results/problem4_improved_fvmbdf/` 只有说明文档＋一份**明确标注「原问题4复制件」的基线表（50.6528h）**，**没有 `result4.xlsx`**。而 B（`agent_B.md:17`）已决定论文 Q4 采用该改进副本口径。**要兑现 B 的决定，就必须真跑一次改进版**；又因附件被 gitignore（见下），**目前只有 agent_A（本机）能跑**。这是本轮唯一带阻塞性质的动作。

---

## 关键背景：被混在一起的「四个层级」

数字之争的根源，是四个不同层级被当成同一件事比较。厘清后，①②③ 的判断就清楚了：

| 层级 | 模型形式 | 界面平均 | 时间推进 | 事件/网格 | 结果状态 | t\* |
|---|---|---|---|---|---|---|
| **L1 baseline** `problem4` | 物质坐标 front-fixing（**无对流**） | k、D 全**算术** | 固定 10s BDF1＋Picard | 离散 60s / 201 点 | **已算**（`result4.xlsx` 235KB） | **50.6528 h** |
| **L2 改进副本（现有代码）** `problem4_improved_fvmbdf` | 物质坐标（**无对流**，`solver.py:132–181` cond/R²、transfer/R） | 水分 D **调和**（`solver.py:226`）、k 仍**算术**（`:209–217`） | 固定 10s BDF1＋Picard（**仍是**一阶，非 adaptive） | 离散 60s / 201 点 | **从未跑**（无 xlsx，表是 L1 复制件） | **未知** |
| **L3 §二十二 最终方案** | 物质坐标（无对流） | k、D **全调和** | **adaptive BDF** | **连续事件** / 801·1601＋Richardson | **尚未编码** | 未知 |
| **L4 §二十五 作者独立 benchmark** | **Euler＋移动坐标输运项（有对流）**（`agent_C.md:1354`） | 调和 | adaptive BDF | 连续事件＋Richardson | 作者本机算过、未入库 | **52.38 h** |

**要点**：L1/L2/L3 都是**物质坐标无对流**（同一物理模型，只差平均方式/时间推进/网格）；**只有 L4 是 Euler＋对流**（不同物理模型）。所以 **52.38h（L4）不能当作 L2/L3 主模型的答案**——它对应的是作者已明确「取代」的旧主方案（`agent_C.md:3189`）。而 B 拍板 Q4 用的是 **L2**，L2 的真实 t\* **谁都还没算过**。

---

## ① 用户总结是否合理 / 有无事实或数值方法判断错误

### A. 我独立复核确认「正确」的核心点（应保留）

1. **front-fixing 坐标变换与链式法则**：ξ=r/R(t) 把移动边界固定到单位圆盘，推导无误。
2. **物质坐标 vs Euler 坐标之辨（最关键）**：在**均匀仿射径向收缩**下，物质点位置 r(t)=ξ·R(t)、ξ 恒定（ξ 即物质点标签），故 ξ 系中材料**静止**、物种守恒方程**无对流项**：∂C/∂t|_ξ = (1/(R²ξ))·∂_ξ(ξ·D·∂C/∂ξ)（＋交换项），R² 来自 r→ξ 映射的 Jacobian 与圆柱散度 (1/r)∂_r(rD∂_r) 的变换。改进副本正是这样实现的（`solver.py:206–207` k_eff=k·inv_r2、d_eff=D·inv_r2；`:215/:224` 表面 h·inv_r、hm·inv_r；**全程无 ξ(Ṙ/R)C_ξ 项**）。**判别性自检有力**：`test_solver.py:87–102` 令 D=0、hm=0，即使 R 收缩，物质点 C 仍逐点守恒（`assert_allclose(out, profile, atol=1e-12)`）——这正是物质坐标的定义性质（Euler 系会出现虚假平流）。**作者从「保留几何输运项」推翻为「物质坐标无对流」是物理上正确的选择**（对仿射收缩而言，若再显式加 ξ(Ṙ/R)C_ξ，等于把已被 cond/R² 吸收的边界运动**重复计入**）。
3. **附录4 变物性**：ρ=760+90C、cp=1850+2150·C/(C+1)、k=0.12+0.20·C/(C+1)、D=4.2e-4·exp(−0.30/C)·exp(−3850/(T+273.15))，代码与文档一致（`solver.py:64–79`、`test_solver.py:56–66`）。
4. **Robin 边界 h/R·hm/R**、**全域阈值事件 g(t)=max_ξ C−0.15=0**（`agent_C.md:3099`）、**输出按 ξ=r/R(t) 回映射、r>R(t) 留空**——均正确且已在代码落地。
5. **对仓库现状的事实描述经我核对全部属实**：两版本对比（`agent_C.md:1634–1636`）、改进副本「无新 `result4.xlsx`、表是 50.6528h 复制件、不能称 50.6528h 为调和改进版答案」（`:1743–1745`、`:1884–1887`）、测试硬编码路径导致 12/13（`:3145`）。这些判断**准确、诚实**。

### B. 两点必须澄清（不是错误，但不写清会误导论文）

1. **52.38h（§二十五）是 Euler＋对流口径，不是最终主模型答案。** 该 benchmark 明列「移动坐标输运项」（`agent_C.md:1354`）＝Euler 型有对流；而 §二十二最终主模型（`:3111` material front-fixing）＝物质坐标无对流。**二者是不同物理模型**。作者自己在「最终判断」里也说物质坐标解释「应当取代我之前默认保留几何输运项的主方案」（`:3189`）。**故 52.38h 应明确标注为「Euler 型 / 模型形式敏感性」参考值，不能作为主模型 t\* 写进论文**；主模型（物质坐标＋调和＋细网格＋adaptive）的收敛 t\* **仍未知**。作者把 52.38h 定位为「参考 benchmark、不要直接写进论文」（`:1345`）是对的，但表6参考值（§二十六 `:1408–1422`）若被人误当改进版结果抄进论文就会出错，务必连同口径一起标注。
2. **50.65h ↔ 52.38h 的 ~1.7h 差混淆了 5 个变量，不是干净的「改进量」。** 二者同时相差：①模型形式（物质无对流 vs Euler有对流，**这是最大混淆项**）②界面平均（算术 vs 调和）③时间推进（固定10s BDF1 vs adaptive BDF）④外推（无 vs Richardson）⑤事件（离散60s vs 连续）。因此**不能把 1.7h 解读为「调和平均带来的改进」**。真正干净的对照应是 **L1（算术）vs L2（仅把水分 D 改调和、其余全同）**——而这个对照**恰恰还没跑过**（见下③-必改2）。

---

## ② 是否赞成「经过完善后的求解方案」（§二十二 + §二十三 12 项任务）

**赞成主干。** §二十二最终方案（物质坐标主模型 + 严格圆柱 FVM + 调和 k,D + adaptive BDF + 连续事件，BE+Picard+TDMA 交叉验证）方向正确；§二十三的 12 项开发任务排序**很扎实、已覆盖我本会提的绝大多数建议**（修测试、k 也调和、确认 FVM 几何、不加对流项＋写明物质坐标假设、网格加密 Richardson/GCI、BE 交叉验证、重生成结果、三类敏感性、收缩消融、最后才替换 50.6528h）。**并且与 B 已拍板的 Q4 口径一致**（`agent_B.md:17`：Q4 用 `problem4_improved_fvmbdf`、明确是一阶 BDF、adaptive BDF 延后且不阻塞本轮论文统一）。

**两点执行调整（针对赛程，不改变方案正确性）：**

- **调整 A：不要把「接入 adaptive BDF」（§二十三.6）当作出结果的前置。** 现有 L2 的 BE+Picard 物质坐标求解器**已可用、且已过 D=0 自检**；直接**加密 xi_points（401→801→1601）＋Richardson/GCI**（§二十三.7）就能拿到主模型收敛 t\*，连续事件（§二十三.6 的一部分）可先用**末两步插值**廉价实现。adaptive BDF 是加分交叉验证、不是必经门槛。**这一点 B 已经拍板同意**（Q4 先按一阶 BDF，adaptive 延后不阻塞），所以调整 A 与仓库现状零冲突。
- **调整 B：附件阻塞在我方可解除，且只能我方解除。** 附件1/2.xlsx 被 gitignore（`.gitignore:62 /附件/`，`git ls-files 附件/` 追踪数=0）——**任何 git clone 都没有附件**，所以 agent_C 说明文档 §5「files/raw 下没有附件 Excel」对 C 而言属实，**C 客观上跑不了 §二十三.9（重生成结果）**。本机 `附件/`（附件1.xlsx 16586B、附件2.xlsx 11485B）由人放置、`run_problem4.py:18–28` 的 `resolve_attachment` 又**优先搜 `附件/`**，故**只有 agent_A 现在能真跑改进版**。要么由我跑出后把 `results/problem4_improved_fvmbdf/` 提交共享，要么请 owner 决定是否取消 gitignore / 另行分发附件让 B、C 也能自跑。

---

## ③ 必改 / 建议改 / 可保留（逐条附证据）

### 必改（4 条）

1. **`test_solver.py:31` 删除/改写硬编码仓库名。** 现断言 `PROJECT_ROOT.name == "MathModeling_Coworkbench_push_tmp2"`（作者开发机文件夹名），换任何机器/文件夹名即失败——这正是 12/13 的唯一失败项（作者 §二十三.2 `:3145` 已知悉）。改成与仓库名无关的断言（如只断言 `MODULE_NAME=="problem4_improved_fvmbdf"` 且 `OUTPUT_DIR==PROJECT_ROOT/"results"/MODULE_NAME`，`test_solver.py:30/32–35` 已覆盖，删掉 :31 即可）。
2. **真跑改进版、产出它自己的 `result4.xlsx`＋表6；在此之前 50.6528h 不得挂「调和/改进版」之名。** 现状：`results/problem4_improved_fvmbdf/` 只有 `problem4_solution_and_code_explanation.md`＋`problem4_tables.md`，后者标题即「原问题4复制件」（`problem4_tables.md:1`）、正文 50.6528h（`:13`），**无 xlsx**。作者 §二十三.9（`:3160`）与 §（`:1745`）已明确此点。**结合 B 已决定 Q4 用该副本，这条从「建议」升级为「阻塞」**：必须真跑一次 L2（水分调和 D、其余同 baseline）得到 L2 的真实 t\*，论文才有正确的 Q4 数字。**只有 agent_A 能跑（附件本地、见②调整B）。**
3. **论文/说明显式写明「轴对称 + 均匀径向收缩 ⇒ ξ=r/R(t) 为物质坐标、主模型无对流项」这一假设**（作者 §二十三.5 `:3151–3152`）。这是全篇最重要的物理判断，必须落到论文假设清单里，并配 D=0 自检（`test_solver.py:87–102`）作为可辩护证据。
4. **补「>4h 环境边界延拓」的长期敏感性。** 40 多小时烘干的后段全靠附件1末值恒定延拓（baseline 假设，`agent_B.md:33` 亦列为非阻塞假设）。这段边界不确定性对 t\* 的影响，**很可能大于「算术 vs 调和」之争**。作者 §二十三.10 提到边界敏感性，但请** specifically 量化 >4h 延拓**（如末值 ±Δ、或不同延拓策略）对 t\* 的影响，并在论文报告区间。

### 建议改（6 条）

5. **k 界面也改调和。** L2 目前水分 D 调和、温度 k 仍算术（`solver.py:209–217` 无 face_average 参数走默认 arithmetic，对比 `:226` 水分显式 harmonic）。作者 §二十三.3（`:3147`）已提。影响小，但与「调和平均界面通量」全文口径（`agent_B.md:17`）一致更好。
6. **网格加密 801/1601 到 |Δt\*|/t\* < 1e-3 ＋ Richardson/GCI**（作者 §二十三.7 `:3156`）。**用现有 BE+Picard 直接加密即可**（见②调整A，不必等 adaptive BDF）。
7. **连续终止事件**（作者 §二十三.6 的一部分）：先廉价实现——在 g(t)=max_ξ C−0.15 变号的**末两步线性插值**求根，比只报离散 60s 时刻更准。
8. **模型形式敏感性（物质 vs Euler，用同一套离散）**：这正好把 §二十五的 52.38h（Euler）纳入为**敏感性对照**，而非孤立数字；论文可报「物质坐标主值 X h，Euler 口径 52.38h，差 Y h」以证物质坐标选择的稳健性。
9. **收缩消融（附录4 固定 R vs 动态 R）**（作者 §二十三.11 `:3165`、§二十四 `:1336`）：单独量化尺寸收缩净贡献，是很好的结果分析素材。
10. **R(t) 插值敏感性（线性 vs PCHIP）**（作者 §二十三.10）：附带说明**物质坐标只需 R(t)、不需 Ṙ(t)**（无对流项），故 R(t) 插值只经 cond/R²、transfer/R 影响，比 Euler 口径对 Ṙ 更不敏感——本身是物质坐标的一个优点，可写进论文。

### 可保留（7 条）

11. 物质坐标无对流项主模型 ＋ D=0 守恒自检单测（`solver.py:132–181`、`test_solver.py:87–102`）——**核心正确，保留**。
12. h/R·hm/R 的 Robin 表面项（`solver.py:215/:224`）——正确，保留。
13. 水分界面调和平均 D（`solver.py:226`、`_face_harmonic_average` `:123–125` safe max 1e-30）——保留。
14. C≥0 裁剪（`solver.py:228`）＋ c_safe=max(C,1e-9)（`:204`）——防负含水率进指数物性，保留。
15. 输出逻辑：固定 r 列 / r>R(t) 留空 / 药材表面列（`run_problem4.py:55–59` `_moisture_at_distance`，d>R(t)→NaN）——保留。
16. `xi_points` 默认 201 已一致（`solver.py:36` 与 `run_problem4.py:148` 均 201）——**修复了 baseline 的 101/201 不一致**（baseline `code/problem4/solver.py:36` 仍 101），保留。
17. `problem4` baseline 原样保留、标注「算术/201/固定10s＝50.6528h」作对照（作者 §二十三.1 `:3141–3142`）——保留。

> 另：§十七 质量一致性诊断 M_d(t)（可选加分、低优先），有余力可补，不作要求。

---

## 执行建议排序（结合「只有 agent_A 能跑」＋ B 的论文计划）

1. **agent_A 先跑 L2**（水分调和 D、xi=201、dt=10，用本机 `附件/`）→ 得 L2 真实 t\* 与 `result4.xlsx`＋表6，提交 `results/problem4_improved_fvmbdf/`。**这一步直接兑现 B 的 Q4 决定**，且解除「50.6528h 被误当改进版答案」的风险。
2. **同一 BE+Picard 求解器加密网格** 401→801→1601 ＋ Richardson/GCI（必改2 的收敛性证据、建议改6）；末两步插值出连续 t\*（建议改7）。
3. **L1（算术）vs L2（调和）干净对照** ＋ **物质 vs Euler 模型形式敏感性**（把 52.38h 收编为对照，建议改8）＋ **>4h 边界延拓敏感性**（必改4）。
4. 时间充裕再做 adaptive BDF 交叉验证、k 调和、收缩消融（②调整A、建议改5/9）——**不阻塞论文**。
5. `test_solver.py:31` 修复（必改1）随手即改，让任何机器都能 13/13。

**一句话**：文档的物理判断（物质坐标）是对的、方案主干我赞成、§二十三任务排序很完整；真正卡住的不是「再讨论方程形式」，而是**改进版从未被真跑过、且只有本机能跑**——这也正是作者「最终判断」末句（`agent_C.md:3216`）说的「优先把 `problem4_improved_fvmbdf` 真正重新跑起来」。请 agent_C 知悉以上 ①②③；是否要 agent_A 现在就跑 L2 并回填结果，我在留言板同步征询队员意见。
